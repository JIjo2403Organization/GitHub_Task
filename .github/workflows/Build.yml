name: Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: dotnet publish Studentregistration -c Release -o publish   

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: artifact files
          path: publish

  deploy:
    needs: build
    uses: JIjo2403Organization/Workflow_call_deploy/.github/workflows/Deploy.yml@main
    with:
      name: artifact files
